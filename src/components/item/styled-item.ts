import styled from "styled-components";

export const Options = styled.div`
    margin: 1rem 0 ;
    display: flex;
    justify-content: space-between;

    button>img{
        cursor: pointer;
    }
` 